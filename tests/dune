(tests
  (names array_like_test)
  (modules array_like_test)
  (libraries varray))

(executable
  (name bench_access)
  (modules bench_access)
  (libraries varray unix))

(executable
  (name exhaust)
  (modules exhaust)
  (libraries varray monolith))
